# 🔒 Настройка Branch Protection в GitHub

## 📋 Инструкция по настройке защиты веток

### 1. Перейди в настройки репозитория

1. Открой свой репозиторий на GitHub
2. Нажми на **Settings** (Настройки)
3. В левом меню выбери **Branches** (Ветки)

### 2. Настрой Branch Protection Rule

1. Нажми **Add rule** (Добавить правило)
2. В поле **Branch name pattern** введи: `main` или `master` (в зависимости от твоей основной ветки)

### 3. Включи следующие опции:

✅ **Require a pull request before merging**

- ✅ Require approvals: `1` (минимум 1 одобрение)
- ✅ Dismiss stale pull request approvals when new commits are pushed

✅ **Require status checks to pass before merging**

- ✅ Require branches to be up to date before merging
- В списке **Status checks** выбери:
  - `lint-and-format` (Lint & Format Check)
  - `build` (Build Check)
  - `test` (Run Tests)
  - `commitlint` (Commit Message Check)

✅ **Require conversation resolution before merging**

✅ **Do not allow bypassing the above settings** (опционально, для строгой защиты)

### 4. Сохрани изменения

Нажми **Create** или **Save changes**

---

## 🎯 Результат

После настройки:

- ❌ **Невозможно** будет смерджить PR без прохождения всех CI проверок
- ❌ **Невозможно** будет смерджить PR без одобрения (code review)
- ❌ **Невозможно** будет пушить напрямую в `main/master` (только через PR)

---

## 🔑 Настройка Secrets для CD

Для автоматического деплоя нужно добавить секреты:

1. Перейди в **Settings** → **Secrets and variables** → **Actions**
2. Нажми **New repository secret**
3. Добавь следующие секреты:

### Для Render:

- `RENDER_API_KEY` - API ключ из Render Dashboard
- `RENDER_SERVICE_ID` - ID сервиса из Render Dashboard

### Как получить Render API Key:

1. Зайди в Render Dashboard
2. Перейди в **Account Settings** → **API Keys**
3. Создай новый ключ и скопируй его

### Как получить Render Service ID:

1. Открой свой сервис в Render
2. В URL будет что-то вроде: `https://dashboard.render.com/web/your-service-id`
3. Или в настройках сервиса найди **Service ID**

---

## ✅ Проверка работы

1. Создай тестовый PR
2. Убедись, что все CI проверки запустились
3. Попробуй смерджить PR - должно быть заблокировано до прохождения всех проверок
4. После прохождения всех проверок PR можно будет смерджить
