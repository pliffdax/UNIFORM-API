# ⚡ Быстрый старт CI/CD

## 🎯 Что нужно сделать:

### 1️⃣ Закоммить и запушить файлы CI/CD

```bash
git add .github/
git add package.json
git commit -m "ci: setup CI/CD workflows"
git push
```

### 2️⃣ Настроить Branch Protection (обязательно!)

1. GitHub → Settings → Branches
2. Add rule для `main`/`master`
3. Включи:
   - ✅ Require pull request before merging
   - ✅ Require status checks to pass
   - Выбери все CI jobs: `lint-and-format`, `build`, `test`, `commitlint`

📖 **Подробная инструкция:** `.github/BRANCH_PROTECTION.md`

### 3️⃣ Настроить Render Auto-Deploy (опционально)

Если используешь Render:

1. Render Dashboard → твой сервис → Settings
2. Включи **Auto-Deploy** для ветки `main`/`master`
3. Выбери **GitHub** как источник

Или используй API деплой (см. `.github/workflows/cd.yml`)

### 4️⃣ Проверить работу

1. Создай тестовый PR
2. Проверь что все CI проверки запустились
3. Попробуй смерджить - должно быть заблокировано до прохождения CI
4. После мерджа проверь что CD запустился

---

## ✅ Готово!

Теперь:

- ✅ Каждый PR проверяется автоматически
- ✅ Мердж возможен только после прохождения всех проверок
- ✅ Автоматический деплой при мердже в main/master

---

## 📚 Документация

- **Полная инструкция:** `.github/README_CI_CD.md`
- **Branch Protection:** `.github/BRANCH_PROTECTION.md`
