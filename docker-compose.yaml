services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: uniflow_user
      POSTGRES_PASSWORD: uniflow_password
      POSTGRES_DB: uniflow_db
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  db_data:

