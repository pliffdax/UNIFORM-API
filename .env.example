# ---------------------------
# APP
# ---------------------------
NODE_ENV=development
PORT=3000

# ---------------------------
# DATABASE (Prisma / Postgres)
# ---------------------------
# Local run (Postgres is published to localhost:5430 by docker-compose.yaml)
DATABASE_URL="postgresql://uniflow_user:uniflow_password@localhost:5430/uniflow_db?schema=public"

# Run inside docker compose (API connects to service 'db:5432')
# DATABASE_URL="postgresql://uniflow_user:uniflow_password@db:5432/uniflow_db?schema=public"

# ---------------------------
# AUTH / JWT
# ---------------------------
JWT_SECRET="change_me_to_a_long_random_secret"

# TTL supports: 15m, 1h, 30d, 500ms etc, or a plain number (seconds)
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=30d

# bcrypt rounds (higher = more CPU)
BCRYPT_ROUNDS=10

# ---------------------------
# OPTIONAL
# ---------------------------
# CORS_ORIGIN="http://localhost:5173"
# LOG_LEVEL=debug
